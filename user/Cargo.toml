[workspace]
resolver = "2"
members = [
    "userlib",
    "programs/console",
    "programs/shell",
    "programs/hello",
    "programs/test",
    "programs/fs-server",
]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
